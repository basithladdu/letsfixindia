---
import SpaShell from "../components/SpaShell.astro";

export function getStaticPaths() {
  return [
    "timeline",
    "landing",
    "statistics",
    "indicators",
    "voices",
    "submit",
    "submissions",
    "faq",
    "methodology",
    "contact",
    "support",
    "about"
  ].map((route) => ({ params: { route } }));
}
---
<SpaShell />
