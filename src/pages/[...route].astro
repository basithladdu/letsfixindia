---
import SpaShell from "../components/SpaShell.astro";
import voices from "../../data/voices.json";

export function getStaticPaths() {
  const namedRoutes = [
    "timeline",
    "landing",
    "gallery",
    "gallery/submit",
    "map",
    "states",
    "statistics",
    "indicators",
    "voices",
    "sources",
    "submit",
    "submissions",
    "faq",
    "methodology",
    "contact",
    "support",
    "partner",
    "donors",
    "about"
  ].map((route) => ({ params: { route } }));
  const voiceRoutes = voices.map((voice) => ({ params: { route: `voices/${voice.id}` } }));

  return [{ params: { route: undefined } }, ...namedRoutes, ...voiceRoutes];
}
---
<SpaShell />
